[project]
name = "yahtzee-autoresearch-evaluation"
version = "0.1.0"
description = "Yahtzee simulation + scoring evaluator."
readme = "README.md"
authors = [
    { name = "Bjorn Kluwe", email = "bjoern.kluwe@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "yahtzee-autoresearch-common",
]

[tool.uv.sources]
yahtzee-autoresearch-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
